/**
 * Complete Auth GraphQL Queries
 * Re-exports all auth query operations needed by auth managers
 */

export { REQUIRES_MFA_QUERY, ME_QUERY, MFA_STATUS_QUERY, IS_MFA_ENABLED_QUERY } from './auth';

// The auth-complete file re-exports from auth.ts which contains all necessary queries
